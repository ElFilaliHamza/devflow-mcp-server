"""Pytest fixtures. The `tmp_data_dir` fixture is defined in Task 2 once
`paths.py` exists; this stub keeps pytest happy in Task 1."""

import pytest


@pytest.fixture
def placeholder():
    return True
